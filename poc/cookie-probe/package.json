{
  "name": "fursoy-cookie-attribute-probe",
  "version": "0.1.0",
  "private": true,
  "description": "MV3 cookie attribute round-trip probe for FURSOY Cookie Protector",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "serve": "node testsite/server.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.2"
  }
}
