// Message table for the "tr" locale. Empty for now — this is the i18n mechanism only; existing
// UI strings are migrated into here incrementally as the popup/options/unlock pages are
// redesigned, not as a separate mechanical pass.
export const tr: Record<string, string> = {};
