# Controlled Session App

Local-only dummy session application for Phase 5 acceptance. It uses an in-memory server-side
session map and an HttpOnly `FCP-mvp-session` cookie. No real credentials or accounts are involved.

```text
npm start
```

Open `http://localhost:43119` and use `mvp-user` / `mvp-password`.
